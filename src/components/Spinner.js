/**
 * @fileoverview Spinner
 * @file A simple spinner that renders while our menu
 * data is fetching.
 */

import React from 'react';

const Spinner = () => {
  return (
    <div className="loading__spinner"></div>
  );
}

export default Spinner;